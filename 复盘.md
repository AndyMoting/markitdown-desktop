# 复盘

## 起因：一个念头太顺了

6 月 3 号晚上看到 markitdown 的文章——各种文档转 Markdown 喂给 AI。脑子里自动补上了 GUI：打包成 exe，拖文件进去，点按钮出 Markdown。不写代码的人也能用。

这个念头顺到我在搜索引擎里敲 `markitdown gui` 之前就开始写代码了。

后来才意识到："没有同类项目"这个信号被我误读了。它可能意味着"需求不存在"，也可能意味着"做过的人都沉默了"——在 vibe coding 时代，后者的概率其实更高。但当时的我根本没停下来想这件事。

---

## 一夜之间：v1 ~ v10

6 月 3 号晚上 11 点开始写 v1，tkinter 最小 demo。跑通了。

然后停不下来。从午夜到凌晨 5 点，连写 9 个版本：

- v2~v3：依赖配置、图片 data URI 转文件、输出子目录
- v4~v7：批量转换、后台线程、进度条、拖拽、PDF 内嵌图片、日志系统、Cancel 按钮、窗口记忆、CLI 模式
- v8~v10：线程安全审查、config 健壮性、死代码清理

凌晨 3 点还在调 PDF 双引擎文字对比的乱码率阈值。AI 写完一段我跑一下，不行就改，改完再跑。完全停不下来——不是项目有多重要，是"马上就做完了"的错觉推着走。

那个 `cancelled=False` 就是凌晨 2 点过的，代码看都没仔细看。

---

## 三天空白：身体在投票

6 月 5、6 号完全没碰。复盘时写"可能是累了，可能是白天有别的事"，语气很淡。

但通宵到早上 5 点的人，接下来两天完全消失，最可能的解释就是身体在强制恢复。这不是"有别的事"，这是生理极限到了。"我休息了两天"比"我把自己熬垮了"好接受，但事实是后者。

这三天其实是第一个被忽略的信号。但当时的我没听懂。

---

## 收尾：v11 ~ v1.0.0

6 月 7 号重新捡起来。v11 抽离引擎，v12 加 .doc 支持，改名 InkDrop——漏改了引擎里的 logger 名字。

6 月 8 号凌晨 v13（实例复用，批量快 30%），v14（自定义输出目录）。凌晨 2 点打出 v1.0.0。

但"重新捡起来"可能根本不是决策。身体缓过来了，惯性还在，就继续了。不是"这件事值得做完"，是"不做完总觉得少了点什么"。

如果 6 月 7 号早上有人帮我把这个文件夹删了，我大概会松一口气。这是第二个被忽略的信号。

---

## 叠在一起的 bug

拖 .doc 进去，转换完，卡住。追了一个多小时：

**Bug 1：日志是空的。** `doc_engine.py` 和 `pdf_engine.py` 的 logger 还在叫 `"markitdown-gui"`，主程序配的是 `"inkdrop"`。名字对不上，引擎错误全部静默丢失。

**Bug 2：状态机假死。**

```
TypeError: after() got an unexpected keyword argument 'cancelled'
```

tkinter 的 `after()` 只接受 `*args`，我传了 `cancelled=False` 当关键字参数。`_on_all_done` 回调从没被调用，`self.running` 永远是 `True`。界面不是卡了，是状态机永远停在"运行中"。

这个 bug 从 v7 就存在，9 个版本全受影响。但被 Bug 1 完美掩盖——异常一直有，只是没人看到。

**Bug 3：打包漏文件。** aspose-words-foss 依赖非 Python 文件 `blank.docx`，PyInstaller 默认不收集。

三个 bug 的叠加很典型：功能 bug + 观测 bug + 部署 bug。单个都不难修，叠在一起全盲。

---

## 为什么搜不到同类项目

做完之后反过来想：vibe coding 一年半了，markitdown 也不是新东西，为什么没人做 GUI 包装？

可能有人做过但 star < 10，搜索引擎排不上。可能仓库是 private。也可能更简单——会用命令行的人不需要 GUI，需要 GUI 的人不知道 markitdown 是什么。两群人不重叠。

但当时的我没想这些。"没有同类项目"被我理解成了"蓝海"，而不是"这里可能没有鱼"。

---

## AI 协作

1300 行代码，90% AI 写的。但"写代码"是最不值钱的部分。

有价值的是决策。PDF 图片要不要、.doc 支不支持、双引擎怎么设计、继续还是收手——每次要对比两个模型的方向，理解本质再选。AI 替代不了这个。

反过来，AI 也让"做"变得太便宜了。以前手写代码要先想三天，现在想到一半 AI 已经写完了。停下来想"值不值得做"反而更难——因为做的阻力太小了。

`cancelled=False` 就是典型：AI 写的，看着像那么回事，我就没查 tkinter 文档。如果是手写，一定会先查签名——因为不记得。

以后让 AI 写完代码，先问自己"这段里我最不确定哪一行"，只查那一行。不查全部，但关键的那一行得自己看。

---

## 结束

坐在电脑前看着跑通的 exe，突然觉得荒诞。

核心功能一行命令就够了：

```powershell
pip install "markitdown[all]"
python -c "from markitdown import MarkItDown; print(MarkItDown().convert('file.pdf').text_content)"
```

GUI、拖拽、进度条、双引擎——对会用命令行的人全是多余的。而不会用命令行的人，大概率不知道 markitdown 是什么，也不知道文档转 Markdown 之后拿来干什么。

需求是编的。"不写代码的人"不是被我发现的需求，是我创造出来的。

---

## 收获

留下来的：

- `文件阅读规范.md`——"以后读文件有参考"，是花了一周买来的认知：如何和 AI 协作处理复杂文档。如果迭代下去，可能比 InkDrop 本身更有复用价值。
- `doc2md.py`——说"真要用的人会用它"，但谁会？会用命令行跑 Python 的人直接 `pip install markitdown` 就行。这个独立 CLI 的受众和 InkDrop 一样模糊。它的价值目前也是假设的，需要下一次使用场景来验证。

从"做一个有用的工具"算，不值。从"踩完从想法到放弃的全周期"算，挺值。之前觉得做出来就是赢——现在知道不做错的事比做好错的事重要得多。

---

## 真正的成本

嘴上说"六七十块 API 钱"，其实在回避更大的数字。

时间至少 15-20 小时。注意力被这个项目占了一整周。凌晨 2 点的判断力比白天差得多，而不好的决策会连锁——比如修完 bug 不睡觉继续改。

只算 API 钱是因为"损失很小，所以没关系"比"我投入了远超它应得的"好接受。

---

## 下次

"会多停一分钟"——但这个决心可能不够。

信号其实出现了多次：不搜索就开始写、通宵到 5 点身体报警、两天自然中断、重新捡起来靠的不是兴奋而是惯性。每次都给了做决策的窗口，但当时的我都滑过去了。

"明天再看看"然后继续——判断力在临界状态时做的决定不可靠。也许需要一个外部机制：发一条消息给朋友"我要做/不做这个，24 小时后如果我没改主意，就开始/放弃"。或者更简单：设一个硬截止时间，凌晨 2 点必须停，不依赖事后的身体罢工。

---

当工具不值。当学费——如果下次能听懂信号的话——很便宜。
